#ifndef FUNCTIONS_H    // if not already defined
#define FUNCTIONS_H

#include "stdafx.h"

int addNum(int x, int y);
int subtractNum(int x, int y);
int multiplyNum(int x, int y);
double divideNum(double x, int y);
int exponentNum(int x, int y);
int factorialNum(int x);

#endif